import { NUsersHopeAreaClient } from './n-usershopearea-client';

describe('NUsersHopeAreaClient', () => {
    it('should create an instance', () => {
        expect(new NUsersHopeAreaClient()).toBeTruthy();
    });
});
