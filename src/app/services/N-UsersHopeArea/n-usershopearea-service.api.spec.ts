import { NUsersHopeAreaServiceAPI } from './n-usershopearea-service.api';

describe('NUsersHopeAreaServiceAPI', () => {
    it('should create an instance', () => {
        expect(new NUsersHopeAreaServiceAPI()).toBeTruthy();
    });
});
