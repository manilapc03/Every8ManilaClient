import { NAccessCountClient } from './n-accesscount-client';

describe('NAccessCountClient', () => {
    it('should create an instance', () => {
        expect(new NAccessCountClient()).toBeTruthy();
    });
});
