import { NAccessCountServiceAPI } from './n-accesscount-service-api';

describe('NUserServiceAPI', () => {
    it('should create an instance', () => {
        expect(new NAccessCountServiceAPI()).toBeTruthy();
    });
});
