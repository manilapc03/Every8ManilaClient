import { NUsersLoginHistoryClient } from './n-usersloginhistory-client';

describe('NUsersLoginHistoryClient', () => {
    it('should create an instance', () => {
        expect(new NUsersLoginHistoryClient()).toBeTruthy();
    });
});
